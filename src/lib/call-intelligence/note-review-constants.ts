// Mirrors LONG_NOTE_CHAR_THRESHOLD in sales-coaching/src/lib/db/call-notes.ts.
// Drift is low-risk; this only affects the "Long note" hint chip in the header.
export const LONG_NOTE_CHAR_THRESHOLD = 2800;
