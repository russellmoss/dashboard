<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Non_Standard_Software_Budget_Required</fullName>
    <active>true</active>
    <description>If the Non-Standard Software Checkbox is checked, then the other fields are required.</description>
    <errorConditionFormula>And(
      Additional_Software__c ,
     OR(
          ISBLANK( Additional_Software_Costs__c )
     ),
     OR(
          ISPICKVAL( StageName, &quot;Joined&quot;),
          ISPICKVAL( StageName, &quot;Signed&quot;)
     )
)</errorConditionFormula>
    <errorMessage>If there is a non-standard software clause as part of the advisor agreements, all of the details must be populated.</errorMessage>
</ValidationRule>
