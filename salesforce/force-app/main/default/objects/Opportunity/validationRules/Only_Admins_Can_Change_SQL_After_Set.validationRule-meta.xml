<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Only_Admins_Can_Change_SQL_After_Set</fullName>
    <active>true</active>
    <description>Only let administrators change the SQL value on an opportunity once it is set by an SGM.</description>
    <errorConditionFormula>AND(
   Not(If(Isnull(PRIORVALUE(SQL__c)),&quot;&quot;,priorvalue(SQL__c))=&quot;&quot;),
   IsChanged(SQL__c),
   $Profile.Name &lt;&gt; &quot;System Administrator&quot;
)</errorConditionFormula>
    <errorMessage>Once an SQL value is set, only an admin can change this field value. Please speak to an administrator for this.</errorMessage>
</ValidationRule>
